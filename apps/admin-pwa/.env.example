# Leave blank to derive the API URL from the host you open the app on (enables LAN/mobile access).
# Set an absolute URL only for staging/production deployments.
VITE_API_URL=
VITE_API_PORT=3000
VITE_APP_ENV=development
VITE_AUTH_BYPASS=false
